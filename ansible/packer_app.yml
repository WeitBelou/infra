---
- name: Initial provisioning of app base image.
  hosts: all
  become: yes

  tasks:
    - name: Install ruby
      apt:
        name: "{{ packages }}"
      vars:
        packages:
          - ruby-full
          - ruby-bundler
          - build-essential
